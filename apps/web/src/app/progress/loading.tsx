import { PageLoadingState } from '../../components';

export default function Loading() {
  return (
    <PageLoadingState
      title="Loading progress"
      description="Fetching live progress trends and coaching signals."
    />
  );
}
